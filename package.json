{
  "name": "portfolio-root",
  "version": "1.0.0",
  "description": "Root orchestration for portfolio (frontend, backend, admin)",
  "scripts": {
    "start": "npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../admin && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:admin": "cd admin && npm run dev -- --port 5174",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
